# Dataset

Do not commit chest X-ray datasets or patient-identifiable data to this repository.

Expected local layout:

```
data/
├── raw/
└── processed/
```

The project will document the selected public research dataset, label schema, split strategy and download instructions once dataset selection is finalized.
