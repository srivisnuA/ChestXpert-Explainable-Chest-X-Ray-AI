# Model artifacts

Large model checkpoints should not be committed to Git. Store reproducible training configuration in the repository and document the checkpoint location/version.

Only commit model weights when their size, license and redistribution terms make that appropriate.
